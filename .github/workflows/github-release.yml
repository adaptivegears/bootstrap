---
name: Release - Preset

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
    tags:
      - "v*.*.*"

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        platform:
          - ansible: amd64
            python: x86_64
          - ansible: arm64
            python: aarch64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build
        uses: docker/build-push-action@v6
        with:
          context: src
          push: false
          load: true
          platforms: linux/${{ matrix.platform.ansible }}
          cache-from: type=gha,scope=linux/${{ matrix.platform.ansible }}
          cache-to: type=gha,mode=max,scope=linux/${{ matrix.platform.ansible }}
          build-args: |
            ANSIBLE_ARCH=${{ matrix.platform.ansible }}
            PYTHON_ARCH=${{ matrix.platform.python }}
            PYTHON_RELEASE=20241016
            PYTHON_VERSION=3.11.10
          outputs: |
            type=local,dest=dist

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: preset-linux-${{ matrix.platform.ansible }}
          path: "dist/*"

  test:
    runs-on: ubuntu-latest
    needs: build

    strategy:
      matrix:
        platform:
          - ansible: amd64
            python: x86_64

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: preset-linux-${{ matrix.platform.ansible }}
          path: dist

      - name: Test
        run: >-
          make test
          ANSIBLE_ARCH=${{ matrix.platform.ansible }}
          PYTHON_ARCH=${{ matrix.platform.python }}

  release:
    runs-on: ubuntu-latest
    needs:
      - build
      - test
    if: startsWith(github.ref, 'refs/tags/v')

    permissions:
      contents: write

    steps:
      - id: prerelease
        run: |
          echo "prerelease=$(if [[ "${{ github.ref }}" == *"-rc"* ]]; then echo "true"; else echo "false"; fi)" >> "$GITHUB_OUTPUT"

      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true
          pattern: preset-linux-*

      - name: Create SHA512SUMS
        run: |
          sha512sum preset-* > SHA512SUMS

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          prerelease: ${{ steps.prerelease.outputs.prerelease }}
          generate_release_notes: true
          files: |
            SHA512SUMS
            preset-*
